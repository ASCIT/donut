The relevant folders are modules/editor and modules/uploads. 

Any users with correct permissions can access the editor page. This allows one to create new pages and edit old ones.

There are no tables associated with these two modules -- in hindsight should've used a table to store the md and upload files.  

